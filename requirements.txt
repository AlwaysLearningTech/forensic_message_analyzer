# Core dependencies for forensic analysis.
#
# This file lists the supported minimum versions. For reproducible installs (required for forensic defensibility), install from requirements-lock.txt, which is pinned to the exact versions used during verification runs.
pandas>=1.5.0
numpy>=1.21.0
PyYAML>=6.0
python-dotenv>=0.19.0

# Image processing and OCR for screenshot analysis
Pillow>=9.0.0
pytesseract>=0.3.10

# File type detection for attachments
python-magic>=0.4.27

# Report generation
openpyxl>=3.0.9  # Excel reports
python-docx>=0.8.11  # Word reports
docx2pdf>=0.1.8  # DOCX-to-PDF conversion (requires MS Word or LibreOffice)

# Data visualization (optional but recommended)
plotly>=5.0.0  # Interactive charts for timeline visualization
matplotlib>=3.5.0  # Static charts as fallback

# AI analysis (Anthropic Claude via direct API or Azure AI)
anthropic>=0.40.0

# Web-based interactive review
Flask>=3.0.0

# Testing
pytest>=7.0.0
pytest-mock>=3.6.0
pytest-cov>=3.0.0

# NLP and text analysis
textblob>=0.17.1  # Sentiment analysis

# Timezone support
pytz>=2023.3

# Trusted CA bundle for SSL verification (used by pricing.py rate lookup;
# falls back to system trust store if missing).
certifi>=2023.7.22

# iMessage attributedBody binary format parsing
pytypedstream>=0.1.0

# PDF text extraction for counseling records
pdfplumber>=0.9.0

# Additional forensic utilities
python-dateutil>=2.8.2  # Date parsing

# HTML report templating and PDF generation
jinja2>=3.1.0  # HTML template rendering
weasyprint>=60.0  # HTML-to-PDF conversion
